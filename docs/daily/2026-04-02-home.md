# 일일 보고서 — 2026-04-02 (집 작업)

## 진행 환경
- 로컬: Windows 11 / D드라이브 venv
- 서버: 집 VM (172.30.1.44)

---

## 작업 내용

### 1. 집 서버 코드 동기화
- 회사에서 진행한 Phase 3-1 ~ 3-4 코드를 `git reset --hard origin/claude/nervous-northcutt`로 맞춤
- 이전 로컬 작업과 충돌 → rebase 포기 후 원격 HEAD 기준으로 초기화
- 회사 버전 코드 (app.py, static/) SCP로 서버 재배포 완료

### 2. Phase 3-3 RAG 베타 구현
- **`rag.py` 신규 작성**
  - ChromaDB + `paraphrase-multilingual-MiniLM-L12-v2` (한국어 지원 임베딩)
  - PyMuPDF로 PDF 텍스트 추출 → 슬라이딩 윈도우 청킹 (500자, 100자 오버랩)
  - `build_index()`, `search()`, `search_to_context()`, `get_status()` 구현
  - Rocky Linux 9 SQLite 버전 이슈 → `pysqlite3-binary` 오버라이드로 해결

- **`app.py` 수정**
  - `search_knowledge` 도구를 `CHAT_TOOLS`에 추가 (챗봇 자동 검색 연동)
  - `/api/rag/status`, `/api/rag/build`, `/api/rag/search` 엔드포인트 추가

- **RAG 인덱스 빌드**
  - 기능분析서 PDF 1개 (69페이지) → 88개 청크
  - 로컬 + 서버 양쪽 `chroma_db/` 구축 완료

- **서버 배포**
  - `chromadb`, `sentence-transformers`, `pysqlite3-binary` 서버 venv 설치
  - `chroma_db/`, `rag.py`, `docs/feature_manual.pdf` 서버 전송
  - `/api/rag/status` → `{"status": "ready", "chunks": 88}` 확인

---

## 발견된 이슈
- **CHAT_SYSTEM_PROMPT에 RAG 검색 지시 누락** — Claude가 `search_knowledge` 도구를 적극 활용하지 않을 수 있음. 시스템 프롬프트에 기능분析서 검색 지시 추가 필요.
- **검색 결과 필터링 없음** — 거리값 관계없이 상위 N개 반환 → 무관한 결과가 Claude를 오도할 수 있음

---

## RAG 정확도 향상 로드맵

### 즉시 수정 (긴급)
| # | 항목 | 설명 |
|---|------|------|
| A | CHAT_SYSTEM_PROMPT 수정 | search_knowledge 도구 사용 지시 추가 |
| B | 검색 거리 임계값 | 코사인 거리 > 0.6 결과 필터링 (1줄 수정) |

### 단기 (Phase 3-3 마무리)
| # | 항목 | 설명 |
|---|------|------|
| 1 | 다중 PDF 인덱싱 | `build_index()` → `docs/` 디렉토리 전체 PDF 스캔 |
| 2 | 7개 매뉴얼 통합 빌드 | 예상 500~700개 청크 |
| 3 | 구조적 청킹 | 500자 고정 → 섹션(2-1-4 등) 단위 청킹. 맥락 보존 대폭 향상 |

### 중기 (Phase 3-3+)
| # | 항목 | 효과 | 설명 |
|---|------|------|------|
| 4 | 정책 DB 자동 인덱싱 | ⭐⭐⭐ | MariaDB 13개 테이블 실제 정책 → ChromaDB 자동 동기화. `/api/rag/sync-db` 엔드포인트 또는 cron |
| 5 | 쿼리 확장 (동의어) | ⭐⭐ | "SZ"→"시큐어존", "RC"→"랜섬크런처" 등 한영/약어 자동 매핑 |
| 6 | 사용자 오답 수정 | ⭐⭐ | 웹 UI에서 수정 → ChromaDB 즉시 반영 + MariaDB 감사 로그. `/api/rag/correct` |

### 장기 (운영 안정화 후)
| # | 항목 | 효과 | 설명 |
|---|------|------|------|
| 7 | 멀티쿼리 RAG | ⭐⭐ | 질문을 2~3가지 변형 검색 → 재현율 향상 (API 비용 증가 감안) |
| 8 | 백업/운영 자동화 | ⭐⭐ | cron 기반 chroma_db 백업 + MariaDB dump + PDF 재빌드 시 수정분 자동 재주입 |

### 데이터 관리 원칙
- **Git**: 코드만 (app.py, rag.py, db.py 등)
- **서버 로컬**: 데이터만 (chroma_db/, docs/*.pdf, .env)
- **.gitignore**: `chroma_db/`, `docs/*.pdf`, `.env`, `venv/`
- **백업**: cron으로 매일 chroma_db + MariaDB dump → /backup/

---

## 남은 작업 (내일 회사에서)
- 나머지 6개 제품 기능분析서 PDF 추가
- `build_index()` 다중 문서 일괄 인덱싱 지원으로 확장
- 7개 문서 통합 인덱스 빌드 후 검색 품질 검증
- CHAT_SYSTEM_PROMPT RAG 지시 추가
- 검색 거리 임계값 필터 적용
